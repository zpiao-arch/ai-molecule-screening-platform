# Open Molecule Lab Third-Party Notice

This initial interaction shell was copied from the local project:

`<validated-workspace>/web/evidence-workbench`

The copied material includes the React/Vite application structure, the evidence-ledger visual design, and the local frontend dependency lockfile. The legacy HelixGuard task runner, its task state, generated `dist/`, `node_modules/`, logs and PID files are intentionally not copied.

The four-level CLI source package is currently source-available rather than an OSI-licensed open-source distribution. React, React DOM, Vite, TypeScript and lucide-react remain governed by their respective upstream licenses; review `package-lock.json` and upstream notices before redistributing a built application.

This app is an initial plan-only prototype. It must not claim that a prompt plan has produced molecular scores or docking evidence.
