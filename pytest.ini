[pytest]
addopts = -m "not integration_assets and not integration_docking"
markers =
    integration_assets: requires the separately distributed model/data/receptor asset archive
    integration_docking: requires executable smina and obabel binaries configured for this platform
